% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/post-gan.R
\name{apply_post_gan_boosting}
\alias{apply_post_gan_boosting}
\title{Apply Post-GAN Boosting to a Trained GAN}
\usage{
apply_post_gan_boosting(
  trained_gan,
  real_data,
  transformer = NULL,
  n_candidates = 1000,
  steps = 400,
  dp = FALSE,
  MW_epsilon = 0.1,
  weighted_average = FALSE,
  averaging_window = NULL,
  device = NULL,
  seed = NULL
)
}
\arguments{
\item{trained_gan}{A trained GAN object of class "trained_RGAN" with checkpoints}

\item{real_data}{The original training data (matrix)}

\item{transformer}{Optional data_transformer for inverse transformation}

\item{n_candidates}{Number of candidate samples to generate per generator. Defaults to 1000.}

\item{steps}{Number of boosting steps. Defaults to 400.}

\item{dp}{Use differential privacy for discriminator selection. Defaults to FALSE.}

\item{MW_epsilon}{Privacy budget for multiplicative weights (only used if dp=TRUE). Defaults to 0.1.}

\item{weighted_average}{Use weighted averaging for final distribution. Defaults to FALSE.}

\item{averaging_window}{Window size for averaging phi distributions. Defaults to NULL (use all steps).}

\item{device}{Device for computation. Defaults to trained_gan's device.}

\item{seed}{Optional seed for reproducibility.}
}
\value{
A list with:
\itemize{
\item samples: Matrix of selected high-quality synthetic samples
\item scores: Discriminator scores for selected samples
\item n_unique: Number of unique samples selected
}
}
\description{
High-level wrapper that orchestrates the full post-GAN boosting workflow:
\enumerate{
\item Generates candidate samples from checkpointed generators
\item Computes discriminator scores using checkpointed discriminators
\item Applies the post-GAN boosting algorithm to select high-quality samples
}
}
\examples{
\dontrun{
# Train a GAN with checkpoints
trained_gan <- gan_trainer(
  transformed_data,
  epochs = 100,
  checkpoint_epochs = 10  # Save every 10 epochs
)

# Apply post-GAN boosting
boosted <- apply_post_gan_boosting(
  trained_gan,
  real_data = transformed_data,
  n_candidates = 5000,
  steps = 200
)

# Use the boosted samples
high_quality_samples <- boosted$samples
}
}
